{
  "name": "caspian-email-functions",
  "version": "0.1.1",
  "description": "Firebase Cloud Functions for Caspian Store transactional email. Reads the enabled email provider plugin from Firestore (`emailPluginInstalls`) and delegates sending — no secrets declared at the function level, so `firebase deploy` does not require any `functions:secrets:set` step.",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions:caspian-email",
    "lint": "echo \"(no lint configured)\"",
    "logs": "firebase functions:log --only caspian-email"
  },
  "dependencies": {
    "@getbrevo/brevo": "^2.2.0",
    "@sendgrid/mail": "^8.1.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "overrides": {
    "@tootallnate/once": "^3.0.1",
    "http-proxy-agent": "^7.0.2",
    "uuid": "^14.0.0",
    "form-data": "^2.5.4",
    "qs": "^6.14.1",
    "tough-cookie": "^4.1.3"
  },
  "private": true
}
