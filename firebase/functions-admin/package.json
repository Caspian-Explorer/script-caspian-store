{
  "name": "caspian-admin-functions",
  "version": "0.2.0",
  "description": "Firebase Cloud Functions for Caspian Store admin triggers (auto-promote, scheduled retention, transactional emails). No Stripe dependency — deployable without Stripe secrets.",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions:caspian-admin",
    "lint": "echo \"(no lint configured)\"",
    "logs": "firebase functions:log --only caspian-admin"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "private": true
}
